steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '${_REPO}/${_IMAGE}:${_TAG}', '-f', '${_DOCKERFILE}', '.']
images:
  - '${_REPO}/${_IMAGE}:${_TAG}'
options:
  logging: CLOUD_LOGGING_ONLY
